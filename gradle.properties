version=5.1.0-RC1

org.gradle.caching=true
org.gradle.parallel=true

# Note: The 'apidocs' and 'publish' tasks may require --no-parallel flag in Gradle 9.3+
# due to stricter configuration resolution locking. Run with:
#   ./gradlew publish --no-parallel

# Test Dependencies
springCloudContractVersion=5.0.3
restAssuredVersion=6.0.1
equalsVerifierVersion=4.5
findbugsVersion=3.0.1u2

# Static Analysis
blockHoundVersion=1.0.17.RELEASE
